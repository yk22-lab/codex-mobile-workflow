# セキュリティ方針

## 公開リポジトリの約束

このリポジトリには秘密情報・個人情報・未公開案件の情報を置きません。設定値が必要なアプリでは、.env.example に項目名だけを書き、実際の値はホスティング先の秘密設定に登録します。

## もし秘密情報を入れてしまったら

1. そのキーやトークンをすぐ無効化・再発行する。
2. 値をコミット履歴から取り除く。
3. 必要なら公開を止め、影響を確認する。

## 脆弱性の連絡

公開Issueには秘密情報を書かず、GitHubアカウント経由で連絡してください。
